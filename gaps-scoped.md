<!-- Scoped gaps: actionable without further discovery. Format:
- [ ] <specific, actionable description>  [<area>] — <why it matters>  #gap-id
-->

- [ ] Verify `weapon_stats_snapshot`, `statChanges`-on-pick, `effect_applied`, `player_stats_snapshot`, and `run_counters_snapshot` events all populate correctly against a live game run (compiles cleanly, dashboard side unit-tested against old data, but nothing observed end-to-end from a real play session)  [plugin/dashboard] — plugin is built but not yet deployed (game was running and locked the DLL during this session); needs a game restart to pick up the new build, then a run played through a shrine/gravestone/encounter to confirm effect_applied actually fires and source attribution is correct  #gap-001
- [ ] `EffectSource.Current` source-attribution in `plugin/Plugin.cs` only covers `InteractableShrineGreed`, `InteractableGravestone`, and `EncounterOffer` — `InteractableShrineBalance`, `InteractableShrineCursed`, `InteractableShrineMagnet`, `InteractableShrineMoai`, and `InteractableShrineChallenge` don't reference `EffectStat` in decompiled source and may apply effects a different way (or not be caught by the ApplyEffect patch at all)  [plugin] — needs empirical testing: trigger each shrine type in game and check whether an `effect_applied` event appears with the right source, or is missing/tagged "unknown"  #gap-004
- [ ] Pause detection (`PauseUi.Pause`/`Resume` patches, `DamagePoller.IsPaused` gate, `tracker.py`'s `elapsed_seconds`/`total_paused_seconds`) is unverified against a live game run — logic is compiled and unit-tested with a synthetic timestamp scenario (confirmed correct arithmetic), but never observed against an actual pause menu open/close in the running game  [plugin/dashboard] — next play session: pause mid-run for a noticeable stretch, then check the damage chart doesn't show a flat stretch and that `run_ended` duration/avg_dps look sane  #gap-005
