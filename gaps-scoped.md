<!-- Scoped gaps: actionable without further discovery. Format:
- [ ] <specific, actionable description>  [<area>] — <why it matters>  #gap-id
-->

- [ ] `EffectSource.Current` source-attribution in `plugin/Plugin.cs` only covers `InteractableShrineGreed`, `InteractableGravestone`, and `EncounterOffer` — confirmed working for `EncounterOffer` (one `effect_applied` event observed live with correct source/stat/amount), but `InteractableShrineBalance`, `InteractableShrineCursed`, `InteractableShrineMagnet`, `InteractableShrineMoai`, and `InteractableShrineChallenge` still unverified (no shrine interaction has appeared in a test session yet)  [plugin] — needs empirical testing: trigger each shrine type in game and check whether an `effect_applied` event appears with the right source, or is missing/tagged "unknown"  #gap-004
- [ ] Run id 19 has total_damage=417614/avg_dps=325.5 with zero rows in `picks` for that run_id — implausible given no other run exceeds ~130 DPS, and no picks logged at all over a 1283s run is inconsistent with every other completed run  [dashboard] — needs investigation into whether `run_started`/`upgrade_picked` events were dropped for this run (check for a gap in `live_events.ndjson` around this run, or a tracker.py exception mid-run) before this run's data can be trusted in any analysis  #gap-007
- [ ] After the 2026-08-18 `EventSink` persistent-FileStream fix, isolated single-frame FPS spikes (`minFps` briefly dropping to ~15-30, `spikeCount=1`) still occur at roughly the same rate before and after a pause/resume cycle (~8-9% of 1s samples in both windows, confirmed via `performance_snapshot` data from a live run) — no longer the sustained "every tick" collapse originally reported (that's fixed, see gaps-done.md), but the smaller residual spikes are unexplained  [plugin] — needs correlation against `.NET` GC events or Unity profiler data during a live run to determine if this is normal GC pause behavior (likely, given it's proportionally stable) or a separate real issue; low priority since it doesn't match the user's originally reported symptom  #gap-008
- [ ] `Patch_PauseUi_Resume` in `plugin/Plugin.cs` only emitted one `game_resumed` event across a 2026-08-19 live session despite the user reporting they paused/resumed the game multiple times — event stream showed one `game_paused` at 00:19:26 and no `game_resumed` for the rest of the session, yet `damage_snapshot` events resumed flowing again around 00:32 anyway. User's lead (2026-08-19): the mismatch seems tied to losing window focus (alt-tab, clicking away) rather than the in-game pause menu, though "doesn't happen all the time" — so it's not a 1:1 focus-change trigger.  [plugin] — undercounts pause/resume cycles, and if `IsPaused` desyncs from what `EventSink.Emit(GameResumedEvent)` actually recorded, `state.is_paused` on the dashboard side (dashboard/tracker.py:104-107) could show "Paused" when the game isn't, or vice versa. **Instrumented, not yet deployed**: added `Patch_AlwaysUi_OnApplicationFocus` (Harmony postfix on the real `AlwaysUi.OnApplicationFocus(bool)`, confirmed via decompile with `[CallerCount(10424)]`) emitting a diagnostic `application_focus_changed` event with `hasFocus` + `isPausedAtTimeOfFocusChange` on every focus change — bundled into the same pending deploy as #gap-013. Once deployed: alt-tab away/back a few times during a run, then grep `live_events.ndjson` for `application_focus_changed` interleaved with `game_paused`/`game_resumed` to see directly whether focus-loss correlates with the missing-resume pattern, and why it's inconsistent. Diagnostic event should be removed once this is resolved.  #gap-011
- [ ] `Patch_GameManager_StartPlaying` in `plugin/Plugin.cs` (line 44-65) hooks `GameManager.StartPlaying`, which calls `EventSink.ResetForNewRun()` and emits a fresh `RunStartedEvent` — the user reported the damage chart reset when reaching stage 2 of a run (2026-08-19), and decompiling `Assembly-CSharp.dll`'s `GameManager` class shows `StartPlaying` has `[CallerCount(4)]` (4 call sites, not 1) plus a separate `A_StageStarted` static event and dedicated stage-timing fields (`nextGetStageTime`, `getMaxStageTimeInterval`, `lastFoundStageTime`) on the same class — strong circumstantial evidence `StartPlaying` fires again on stage transitions within a single run, not only at the true run-start menu action (IL2CPP interop stubs don't expose the method body directly, so this couldn't be confirmed from the decompile alone)  [plugin] — if confirmed, every stage transition (not just deaths) is wrongly wiping the whole run's tracked history (damage chart, picks, effects) and starting a new DB row, which would also explain unexpectedly short/duplicate-looking runs in `runs.db` (see #gap-006, #gap-007 — worth re-checking those against this once confirmed); needs a live repro: tail `live_events.ndjson` while intentionally advancing to stage 2 and check whether a second `run_started` event fires mid-run. If confirmed, the fix is likely swapping the hook to whatever method fires only once per true run start (or adding a guard using `A_StageStarted` to distinguish stage transitions from run start) rather than patching `StartPlaying` itself  #gap-010
- [ ] Deploy the pending plugin/dashboard changes (2026-08-19) — three bundled changes, all compile clean but blocked on the same DLL lock: (1) `GetMaxLevel()` capture across `plugin/Events.cs`, `plugin/Plugin.cs`, `plugin/SimpleJson.cs`, `dashboard/tracker.py`, `dashboard/db.py`, `dashboard/templates/index.html`, `dashboard/templates/run_detail.html` (adds `maxLevel`/`max_level` end-to-end: `IUpgradable.GetMaxLevel()` → `upgrade_picked` event → `picks.max_level` column with migration → `Lv{level}/{maxLevel}` display), (2) the `#gap-011` focus-change diagnostic (`Patch_AlwaysUi_OnApplicationFocus` → `application_focus_changed` event), (3) DPS-vs-enemy-HP tracking (user request, 2026-08-19): `EnemyManager.Instance.enemies` (confirmed real via decompile, live `Dictionary<uint, Enemy>` with `hp`/`maxHp` fields) polled once/sec alongside the existing damage tick, emitting `enemy_health_snapshot` (`totalHp`, `avgHp`, `enemyCount`); dashboard computes instantaneous DPS (delta damage / delta t, not cumulative avg) and a `dps_to_hp_ratio` shown live as "X.X%/s" on the header stat row and as a per-run average on `/history/{id}`; new `enemy_health_history` table (`dashboard/db.py`) with `add_enemy_health_snapshot`/`get_enemy_health_history`. `dotnet build -p:GameDir=...` failed to copy the DLL because Megabonk had it locked (a run was in progress); dashboard-side Python/Jinja changes syntax/template-checked clean but not live-tested against real data yet. Needs: (1) close Megabonk, (2) rebuild the plugin from `plugin/`, (3) restart the dashboard process (`.venv/Scripts/python.exe main.py` from the `.venv`, not a stray system-python copy — see #gap-014 for why that matters), (4) verify a fresh `upgrade_picked` event carries a non-zero `maxLevel`, a focus change produces `application_focus_changed`, and `enemy_health_snapshot` events appear with sane `totalHp`/`enemyCount` (and that `EnemyManager.Instance` isn't null outside active combat, e.g. on menus) in `live_events.ndjson`, (5) the `dps_to_hp_ratio < 5%/s` red-highlight threshold on the live stat tile (`dashboard/templates/index.html`) is a provisional guess, not calibrated against real data — revisit once a few runs' worth of `enemy_health_history` exists  [plugin/dashboard] — the max-level capture unblocks real weapon/tome level caps (40 weapon / 99 tome per `docs/game-limits.md`, now wiki-confirmed rather than just recollection — worth double-checking against live `GetMaxLevel()` data anyway since the wiki itself is fan-maintained); the focus diagnostic resolves #gap-011; the enemy-HP ratio is meant to help identify builds whose DPS falls behind enemy scaling before they die  #gap-013
- [ ] Restarting the dashboard via `nohup .venv/Scripts/python.exe main.py &` under Git Bash on Windows can spawn two processes with the same start timestamp — one running the correct `.venv` interpreter, one running the system `Python312\python.exe` — and it's not deterministic which one ends up bound to port 8420 (observed twice, 2026-08-19: once the system-python copy won the bind and silently ran with a stale/crashed event loop for ~50 minutes while the venv copy sat idle, freezing the live dashboard with no visible error). Likely a Git Bash fork/exec quirk specific to backgrounding a venv shim on Windows  [dashboard] — after any dashboard restart, verify with `Get-NetTCPConnection -LocalPort 8420 -State Listen` that exactly one process owns the port and that it's the expected `.venv\Scripts\python.exe`, not just that *a* process responds to a request. A background `&`-launch without this check can silently leave the dashboard non-functional even though "it started fine." Consider switching the launch method (a `.bat`/`.ps1` wrapper calling `.venv\Scripts\python.exe` directly, or `EnterWorktree`-style foreground launch) to avoid the fork/exec ambiguity entirely  #gap-014
- [ ] Capture several more real-but-unexposed game states found via decompile 2026-08-19/2026-08-20 (see `docs/game-limits.md` "Quiet mechanics" section for full detail) — all trivially callable static/instance calls, no method-body access needed:
  - **Highest priority — settles the weapon/tome cap question outright**: `InventoryUtility.MAX_WEAPON_LEVEL_BASE` / `MAX_TOME_LEVEL_BASE` (static fields) and `GetWeaponMaxLevel()` / `GetTomeMaxLevel()` / `GetNumExtraWeaponLevels()` / `GetNumExtraTomeLevels()` (static methods) — read all four once per run (they're likely constant per-run, not per-tick) to get the real base cap, effective cap, and how much of the gap between them a given run's items have unlocked. Removes the need to infer caps from `IUpgradable.GetMaxLevel()` per-weapon at all.
  - `InventoryUtility.GetNumAvailableWeaponSlots()` / `GetNumMaxWeaponSlots()` (+ tome equivalents) — precise slot counts, better than the plain `isMaxed` booleans.
  - `InventoryUtility.CanUnlockWeapons()` / `CanUnlockTomes()` / `CanUnlockItem(IUpgradable)` — likely what actually gates whether new-weapon/new-tome picks can still appear in the offer pool.
  - `WeaponInventory.isMaxed` / `TomeInventory.isMaxed` and `IsMaxLevel(EWeapon)` / `IsMaxLevel(ETome)` — direct "still finding new weapons/tomes" vs. "fully committed, now leveling" signal instead of inferring it from pick names.
  - `EnemyManager.GetNumMaxEnemies()` / `HasMaxEnemies()` — confirms whether a run has hit the concurrent-enemy cap, which should show up as enemy-HP growth flattening in the new `enemy_health_history` data even as `Difficulty` keeps climbing.
  - `EnemyManager.IsFinalSwarm()` — the user confirmed the trigger (10-minute timer OR all bosses killed) directly, so this is now about capturing the precise moment/duration rather than discovering the trigger; poll it and log a `final_swarm_started` event the first time it flips true.
  - `UpgradePicker.moaiLuckMode` (after a `SetMoaiLuck` call) and whatever event/state marks a Shady Guy interaction (`A_ShadyGuyDone`) — both are item-grant mechanisms distinct from the shrine stat-effect path `#gap-004` already tracks, so they likely need a different event type than `effect_applied` to capture at all (see `docs/game-limits.md` for detail) — this is genuinely new tracking, not just exposing an existing value.
  [plugin] — each is a small, independent addition to the existing 1s poll tick (same pattern as `EmitEnemyHealthSnapshot`) except the Moai/Shady Guy items, which need their own Harmony patches on whatever calls `GetRandomItemsMoai`/`GetRandomItemsShadyGuy`; do them together in one deploy pass rather than one at a time given the DLL-lock friction around deploys  #gap-016
